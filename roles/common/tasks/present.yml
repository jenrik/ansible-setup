---
- name: Add shell snippets
  ansible.builtin.copy:
    src: "{{ item }}"
    dest: "~/.sh.d/{{ item }}"
    owner: "{{ ansible_user_uid }}"
    group: "{{ ansible_user_gid }}"
    mode: u=rwx,go=rx
  loop:
    - "50-mkcd.sh"
    - "50-clipboard.sh"
